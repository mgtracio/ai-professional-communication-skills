# Incident Update Examples

```text
Impact: checkout requests are failing for a subset of UAT users.

Status: investigation is active.

Known facts: the member-details call succeeds, but the downstream callback has not arrived.

Unknowns: whether the provider is rejecting the bet placement request or not sending it.

Owner: payments team.

Next update: 30 minutes.
```
